name: A workflow of my Hello World file
on: push

jobs:
  build:
    name: Hello World action
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v1
      - uses: ./action-a
        with:
          MY_NAME: "HUAN"
